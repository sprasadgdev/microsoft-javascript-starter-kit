// Declare a new variable named petDog and give it the name Rex.
let petDog = "Rex";
// Declare a new variable named petCat and give it the name Pepper.
let petCat = "Pepper";
// Log the petDog variable to the console.
console.log(petDog);
// Log the  petCat variable to the console.
console.log(petCat);

// Log the following to the console: the text "My pet dog's name is: " and the petDog variable.
console.log("My pet dog's name is: " + petDog);
// Log the following to the console: the text "My pet cat's name is: " and the petCat variable.
console.log("My pet cat's name is: " + petCat);
// Declare another variable and name it catSound. Assign the string of "purr" to it.
let catSound = "purr";
// Declare another variable and name it dogSound. Assign the string of "woof" to it.
let dogSound = "woof";
// Log the following to the console: the variable petDog, then the string "says", then the variable dogSound.
console.log(petDog + " says " + dogSound);

// Log the following to the console: the variable petCat, then the string "says", then the variable catSound.
console.log(petCat + " says " + catSound);
// Reassign the value stored in catSound to the string "meow".
catSound = "meow";
// Log the following to the console: the variable petCat, then the string "now says", then the variable catSound.
console.log(petCat + " now says " + catSound);
